# Detection test fixtures and vendored rule data contain intentional, fake
# secret-shaped strings used to exercise bleep's own scanners. Exclude them
# from secret scanning so they don't raise alerts or block pushes.
paths-ignore:
  - "fixtures/**"
  - "rules/**"
  - "tests/**"
  - "src/bin/bench_detect.rs"
  - "docs/**"
