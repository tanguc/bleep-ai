[package]
name = "bleep-events"
version = "0.1.0"
edition = "2021"
description = "Shared wire-event types between bleep-gateway, bleep-tui and the menu-bar GUI"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
