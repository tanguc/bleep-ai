/target
.env
*.jsonl
.tokensave

# runtime / local-only
bleep-stats.db
bleep-stats.db-shm
bleep-stats.db-wal
**/.DS_Store

# machine-specific install state written by the wrapper at runtime
.claude-path

# python tooling artifacts
__pycache__/
*.pyc
.eval-venv/

# editor / tooling state
.task/
claude-wrapper.sh~

# project-local config (machine-specific)
.claude/
.planning/
